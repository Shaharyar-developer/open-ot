{
  "name": "@open-ot/core",
  "version": "0.2.2",
  "description": "",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Shaharyar-developer/open-ot"
  },
  "private": false,
  "scripts": {
    "test": "vitest run",
    "build": "rm -rf dist && bun build --target node src/index.ts --outdir dist && tsc --emitDeclarationOnly --outFile dist/index.d.ts && bun run scripts/dist-package.json.ts && cp README.md dist/README.md",
"publish": "bun run build && npm publish ./dist --access public"
  },
  "devDependencies": {
    "@open-ot/typescript-config": "workspace:*",
    "@open-ot/eslint-config": "workspace:*",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "ot-json1": "^1.0.2"
  }
}
