---
title: Server API
description: Autogenerated API reference for the `@open-ot/server` package.
---

### DocumentRecord

<AutoTypeTable path="/home/shaharyar/Code/Projects/open-ot/packages/server/src/interfaces.ts" name="DocumentRecord" />

### IBackendAdapter

<AutoTypeTable path="/home/shaharyar/Code/Projects/open-ot/packages/server/src/interfaces.ts" name="IBackendAdapter" />

# MemoryBackend

### Constructor


```ts twoslash
import { MemoryBackend } from "@open-ot/server";
// ---cut---
const instance = new MemoryBackend();
//                      ^?
```
### Methods

#### `createDocument(docId, type, initialSnapshot)`

```ts twoslash
import { MemoryBackend } from "@open-ot/server";
// ---cut---
const api = new MemoryBackend();
api.createDocument("", "", undefined as any);
// ^?
```

**Parameters:**
- `docId: string`
- `type: string`
- `initialSnapshot: unknown`

**Returns:** `Promise<void>`

#### `getRecord(docId)`

```ts twoslash
import { MemoryBackend } from "@open-ot/server";
// ---cut---
const api = new MemoryBackend();
api.getRecord("");
// ^?
```

**Parameters:**
- `docId: string`

**Returns:** `Promise<DocumentRecord>`

#### `getHistory(docId, start, end)`

```ts twoslash
import { MemoryBackend } from "@open-ot/server";
// ---cut---
const api = new MemoryBackend();
api.getHistory("", 0, 0);
// ^?
```

**Parameters:**
- `docId: string`
- `start: number`
- `end: number | undefined`

**Returns:** `Promise<unknown[]>`

#### `saveOperation(docId, op, newRevision)`

```ts twoslash
import { MemoryBackend } from "@open-ot/server";
// ---cut---
const api = new MemoryBackend();
api.saveOperation("", undefined as any, 0);
// ^?
```

**Parameters:**
- `docId: string`
- `op: unknown`
- `newRevision: number`

**Returns:** `Promise<void>`


# Server

### Constructor


```ts twoslash
import { Server } from "@open-ot/server";
// ---cut---
const instance = new Server(undefined as any);
//                      ^?
```
**Parameters:**
- `backend: IBackendAdapter`

### Methods

#### `registerType(type)`

```ts twoslash
import { Server } from "@open-ot/server";
// ---cut---
const api = new Server(undefined as any);
api.registerType(undefined as any);
// ^?
```

**Parameters:**
- `type: OTType<Snapshot, Op>`

#### `submitOperation(docId, op, revision)`

Handle an operation submitted by a client.

```ts twoslash
import { Server } from "@open-ot/server";
// ---cut---
const api = new Server(undefined as any);
api.submitOperation("", undefined as any, 0);
// ^?
```

**Parameters:**
- `docId: string`
- `op: unknown`
- `revision: number`

**Returns:** `Promise<{ op: unknown; revision: number; }>`


