---
title: Client API
description: Autogenerated API reference for the `@open-ot/client` package.
---

### OTClientOptions

<AutoTypeTable path="/home/shaharyar/Code/Projects/open-ot/packages/client/src/client.ts" name="OTClientOptions" />

# OTClient

### Constructor


```ts twoslash
import { OTClient } from "@open-ot/client";
// ---cut---
const instance = new OTClient<unknown, unknown>(undefined as any);
//                      ^?
```
**Parameters:**
- `options: OTClientOptions<Snapshot, Op>`

### Methods

#### `subscribe(callback)`

```ts twoslash
import { OTClient } from "@open-ot/client";
// ---cut---
const api = new OTClient<unknown, unknown>(undefined as any);
api.subscribe(() => {});
// ^?
```

**Parameters:**
- `callback: (snapshot: Snapshot) => void`

**Returns:** `() => void`

#### `applyLocal(op)`

Apply a local operation generated by the user.

```ts twoslash
import { OTClient } from "@open-ot/client";
// ---cut---
const api = new OTClient<unknown, unknown>(undefined as any);
api.applyLocal(undefined as any);
// ^?
```

**Parameters:**
- `op: Op`

**Returns:** `Op | null`

#### `serverAck()`

Handle an acknowledgement from the server.

```ts twoslash
import { OTClient } from "@open-ot/client";
// ---cut---
const api = new OTClient<unknown, unknown>(undefined as any);
api.serverAck();
// ^?
```

**Returns:** `Op | null`

#### `applyRemote(op)`

Apply a remote operation from the server.

```ts twoslash
import { OTClient } from "@open-ot/client";
// ---cut---
const api = new OTClient<unknown, unknown>(undefined as any);
api.applyRemote(undefined as any);
// ^?
```

**Parameters:**
- `op: Op`

**Returns:** `Op`

#### `getSnapshot()`

```ts twoslash
import { OTClient } from "@open-ot/client";
// ---cut---
const api = new OTClient<unknown, unknown>(undefined as any);
api.getSnapshot();
// ^?
```

**Returns:** `Snapshot`

#### `getRevision()`

```ts twoslash
import { OTClient } from "@open-ot/client";
// ---cut---
const api = new OTClient<unknown, unknown>(undefined as any);
api.getRevision();
// ^?
```

**Returns:** `number`


