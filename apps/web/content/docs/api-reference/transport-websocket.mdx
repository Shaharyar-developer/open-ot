---
title: Transport Websocket API
description: Autogenerated API reference for the `@open-ot/transport-websocket` package.
---

# WebSocketTransport

### Constructor


```ts twoslash
import { WebSocketTransport } from "@open-ot/transport-websocket";
// ---cut---
const instance = new WebSocketTransport<unknown>("");
//                      ^?
```
**Parameters:**
- `url: string`

### Methods

#### `connect(onReceive)`

```ts twoslash
import { WebSocketTransport } from "@open-ot/transport-websocket";
// ---cut---
const api = new WebSocketTransport<unknown>("");
api.connect(() => {});
// ^?
```

**Parameters:**
- `onReceive: (msg: M) => void`

**Returns:** `Promise<void>`

#### `send(msg)`

```ts twoslash
import { WebSocketTransport } from "@open-ot/transport-websocket";
// ---cut---
const api = new WebSocketTransport<unknown>("");
api.send(undefined as any);
// ^?
```

**Parameters:**
- `msg: M`

**Returns:** `Promise<void>`

#### `disconnect()`

```ts twoslash
import { WebSocketTransport } from "@open-ot/transport-websocket";
// ---cut---
const api = new WebSocketTransport<unknown>("");
api.disconnect();
// ^?
```

**Returns:** `Promise<void>`


